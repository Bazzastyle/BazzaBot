<?php
  /**
   * @var BazzaBot\Client $client
   * @var BazzaBot\EasyVars $tg
   * @var BazzaBot\Database $db
   * @var string $symbol
   */

  if ( $tg->chat_type === 'private' ) {

  }
  else {
    
  }